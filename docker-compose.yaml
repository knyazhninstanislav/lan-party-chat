services:
  lan-party:
    build: .
    container_name: lan-party
    ports:
      - "5000:5000"
    volumes:
      - lan-party-data:/app/data
    environment:
      - PORT=5000
      - DB_PATH=/app/data/chat.db
    restart: unless-stopped

volumes:
  lan-party-data: